import { StyleSheet } from "react-native";

const text = StyleSheet.create({
    p: {
      color: 'black',
      fontSize: 14,
    },
    title: {
      fontWeight: 'bold',
      color: 'black',
      fontSize: 20,
    },
    subTitle: {
      fontWeight: 'bold',
      color: 'black',
      fontSize: 14,
    },
    data: {
      color: 'black',
      fontSize: 25,
    },

  });
export default text;
